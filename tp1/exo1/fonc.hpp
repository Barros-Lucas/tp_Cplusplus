#include <string>

namespace fonctions{
    size_t strlen_(const char* st);
    void strcpy_(char* destination, const char* source);
    char* strncpy_(char *destination, const char *source, size_t size);
}
